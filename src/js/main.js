import { Header } from './components/header';

document.addEventListener("DOMContentLoaded", () => {
    const header = new Header("#app");
    header.setTitle("Bienvenido a mi Boilerplate Vanilla JS");    
});